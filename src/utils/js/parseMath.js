export const parseAdd = (currentAmount, inputVal) =>
  parseInt(currentAmount, 10) + parseInt(inputVal, 10);

export const parseMinus = (currentAmount, inputVal) =>
  parseInt(currentAmount, 10) - parseInt(inputVal, 10);

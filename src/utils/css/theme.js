import { colors } from './cssColors';
import { bp } from './cssBreakpoints';

export const header = {
  primaryBlue: `${colors.background.quartiary}`,
  primaryLight: `${colors.background.tertiary}`,
  primaryDarkblue: `${colors.text.primaryHeadline}`,
  primaryHover: `${colors.background.quintiary}`,
  mobile: `${bp.small}`,
};
